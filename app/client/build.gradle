dependencies {
    compile project(":component:ping-messages")
    compile project(":component:web")
}

bootRepackage {
    mainClassName = 'io.zerodi.client.ClientApplication'
}

jar {
    baseName = 'app-client'
    excludes = ['**/logback.groovy']
}
